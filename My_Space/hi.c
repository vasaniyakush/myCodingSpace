#include<stdio.h>
#include<conio.h>
#include<string.h>
int main(){
    char stra[] = "", strb[20] = "world";
    // printf( "%s",strcpy(strb,strcat(stra,strb)) );
    if (printf("%s",stra))
    {
        printf( "a;kldja;ld" );
    }else printf( "ajsdfgsh" );
    
    // ary[][] = {{1,2,3,},{4,5,6}};
    // printf( "%d",ary[1][0] );
    
    
return(0);
}